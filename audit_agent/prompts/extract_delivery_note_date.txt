你是一位专业的工程文档结构化处理专家。请从以下 OCR 识别后的纯文本中，**为每一份独立的工程文件提取其对应的日期信息**。

**独立文件判断依据**：  
若文本中出现以下任一情况，视为新文件的开始：
- 明确分隔符（如 “——— 下一页 ———”、`***`、连续换行+重复标题等）；
- 重复出现典型文件标题（如 “工程联系单”、“材料进场报审表” 等）；
- 独立的编号体系（如两个不同的“编号：XXX”）；
- 页码重置或明显语义断点。

**对每一份独立文件，请执行以下操作：**
1. 在该文件的**主信息区域**（正文、表头、签章区附近）查找所有符合常见日期格式的字符串，包括：
   - 带引导词的值，如 “送货日期：2025.3.15” → 提取 `2025.3.15`
   - 无引导词但格式明显的字符串，如 `2024年12月1日`、`3月5日`、`2026/01/22`、`2025.1.22` 等
2. **仅当该日期出现在当前文件的上下文中**，才归属到该文件。
3. **必须逐字保留 OCR 原始格式**，不得修正、补零、转换或标准化。
4. **排除模板文字**：如仅有“日期：”而无后续内容，则不提取。
5. 若某文件中未找到任何有效日期，则其 `dates` 字段为空数组。

**输出格式：**
- 输出一个 JSON 数组；
- 每个元素是一个 JSON 对象，格式为：`{"dates": [ "日期1", "日期2", ... ]}`
- 不包含任何额外文本、注释、说明或 Markdown；
- 若无任何有效文件，返回 `[]`

**输入：**
```
{ocr_result}
```
/no_think